
//Function Expression:
const fExpresion = function(a=1, b=2, c=3){
    const suma = a + b + c;
    console.log( `El valor de la suma expresado en esta Función de Expresión es: ${suma}` );
}
fExpresion();